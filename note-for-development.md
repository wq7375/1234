> 这是我个人使用的note，用于记录一些事项--cyz

## 计划

* 写`README.md`(可选).

### 页面`courseManage`的逻辑修改

* 修改删除课程的逻辑
* 添加提醒保存功能
  > 暂定实现逻辑：给页面添加一个属性`isEdited`，在进行页面操作和保存时，修改这一属性的值。

### 一些GUI的修改

* 页面`courseManage`中日期和课程类型的对齐方式
* 页面`schedules`中选课名单的显示

## 疑问

定义了云函数`checkClassStatus`，但没有见到调用，这是为何啊？